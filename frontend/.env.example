# Local dev only. In production (S3 + CloudFront) the CI workflow passes
# VITE_API_URL=http://<EC2_HOST>:5000 to `vite build`, which bakes it into
# the static bundle at build time.
VITE_API_URL=http://localhost:5000
